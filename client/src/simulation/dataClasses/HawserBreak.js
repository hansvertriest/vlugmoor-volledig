export default class HawserBreak {
    constructor(hawserId, timePointIndex, loadRatio) {
        this.hawserId = hawserId;
        this.timePoint = timePointIndex;
        this.loadRatio = loadRatio;

        this.loadRatio = loadRatio;
    }

}